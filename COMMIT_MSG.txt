fix: tools must declare output{schema,render} for the real DSH defineTool

Same host crash as dsh-grok-memory: defineTool reads options.output.render
unguarded, so a tool without an output block throws in apply() as soon as
the host loads the plugin.

- every tool now passes output:textOutput (schema + render)
- test stub mirrors the real contract instead of passing anything through
- verified against the real defineTool extracted from app.asar: all 5 tools
  construct successfully; plugin test suite 32/32 green
